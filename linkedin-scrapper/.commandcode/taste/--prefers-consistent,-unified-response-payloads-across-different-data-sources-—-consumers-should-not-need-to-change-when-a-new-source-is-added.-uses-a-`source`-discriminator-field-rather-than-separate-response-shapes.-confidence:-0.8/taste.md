# - Prefers consistent, unified response payloads across different data sources — consumers should not need to change when a new source is added. Uses a `source` discriminator field rather than separate response shapes. Confidence: 0.8
- Prefers consistent, unified response payloads across different data sources — consumers should not need to change when a new source is added. Uses a `source` discriminator field rather than separate response shapes. Confidence: 0.8
- Prefers unified API input schemas across different backends — callers send the same request parameters regardless of source, and internal translation layers handle source-specific differences rather than exposing them to consumers. Values a feasibility analysis first ("what fields can be possible and what not") before implementing the translation. Once feasibility is established, makes quick, pragmatic triage decisions (e.g., "add X, skip Y") rather than debating edge cases. Confidence: 0.3 — NOTE: later reversed; user found the shared/translated approach confusing and moved to source-specific native schemas (see below).
- Prefers source-native, independent parameter schemas for each data source rather than a shared/translated input layer. Each Lambda/endpoint accepts only its own fields — no translation, no shared params. Callers are expected to know which source they're calling and send source-appropriate parameters. The response shape remains unified (same `JobPosting` with `source` discriminator), but input schemas are independent. Confidence: 0.85
- Prefers keeping related functionality in the same Lambda rather than splitting into separate micro-Lambdas, until there is a concrete reason to split (e.g., different memory/timeout needs). When splitting, keeps shared code in common modules that esbuild tree-shakes per Lambda — no duplication. Confidence: 0.85
- Prefers self-hosted, infrastructure-level solutions (Puppeteer/chromium) over paid external API dependencies (Apify) for scraping, accepting the operational burden of browser automation to avoid per-request costs and vendor lock-in. Confidence: 0.7
- Values understanding the technical architecture and flow — asks clarifying questions both before implementation (to align on approach) and after (to understand what was built and why). Prefers clear, structured explanations with rationale, not just results. Confidence: 0.75
- Prefers test scripts to follow existing patterns in the codebase — when adding a new test, explicitly asks to mirror the approach used by existing tests rather than inventing a new style. Also prefers dedicated npm scripts for running tests. Confidence: 0.75
- Wants exhaustive field-level gap analysis between data sources — when a subset of unmapped fields is discussed, proactively asks about all remaining fields ("and any other fields which we are not exposing"). Prefers a complete compatibility table over incremental, piecemeal answers. Confidence: 0.7
- Prefers comprehensive, reference-style documentation — READMEs should enumerate every parameter, its type, whether it's required, all allowed values, and which sources support it. Documentation should be complete enough to serve as the primary integration guide without needing to read source code. Confidence: 0.8
- When a parameter exists in both a shared/simple form and a source-specific raw-override form (e.g., `sortBy` vs `sort`), wants the documentation to put the "just use this" recommendation front and center — the override params should be clearly labeled as escape hatches so users aren't confused about which to pass. Prefers a prominent banner rather than burying the guidance in a table cell. Confidence: 0.5 — NOTE: the shared/override pattern was later abandoned entirely in favor of independent schemas, so this preference is now moot rather than wrong.
- Prefers hands-on, empirical feasibility testing over theoretical analysis — when evaluating whether a new platform or approach will work, wants to actually hit it with real browser automation and see the results rather than speculating based on known limitations. Confidence: 0.7
- Prefers using his own Chrome profile (with existing logins, cookies, and sessions) for browser automation tasks — values the more accurate feasibility signal from authenticated, real-world browsing sessions over clean/blank headless instances. Confidence: 0.7
- Prefers the simplest viable implementation approach — before committing to browser automation (Puppeteer), asks whether a plain HTTP fetch/curl would work. Values reducing operational complexity (no browser to manage, faster, cheaper) when a site's SSR or API surface allows it. Only reaches for Puppeteer when simpler approaches are proven infeasible. Confidence: 0.8
- Requires core essential fields — especially job descriptions — to consider a data source viable. Makes quick, decisive cutoff calls: if a source cannot deliver a must-have field (e.g., Glassdoor requires sign-in for detail pages), drops it entirely rather than pursuing partial data or workarounds. Confidence: 0.6
- Prefers phased, sequential implementation with simpler sources first — get the lightweight/axios-based sources working end-to-end before tackling complex Puppeteer/browser-automation sources. Explicitly defers harder work ("after they are working, let's go with Indeed") rather than parallelizing everything at once. Confidence: 0.7
- Avoids the `random-useragent` npm package for HTTP scraping — it emits obsolete/archaic UA strings (BlackBerry, Safari 3.1, etc.) that modern Cloudflare/WAF deployments reject with 403. Prefers a single fixed, modern Chrome User-Agent constant (e.g., Chrome 131) shared across all axios-based scrapers. Confidence: 0.7
